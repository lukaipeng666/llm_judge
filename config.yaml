# 数据库服务配置
database_service:
  host: "0.0.0.0"
  port: 16384
  database_url: "data/llm_judge.db"

# Web API 服务配置
web_service:
  host: "0.0.0.0"
  port: 16385
  database_service_url: "http://localhost:16384"

# 前端服务配置
frontend_service:
  port: 16386
  host: "0.0.0.0"

# Redis 服务配置
redis_service:
  host: "localhost"
  port: 16387
  db: 0
  max_wait_time: 300  # 最大等待时间（秒）

# JWT 配置
jwt:
  secret_key: "llm-judge-secret-key-change-in-production"
  expire_hours: 24

# LLM 服务配置
llm_service:
  api_url: "http://localhost:8000/v1"
  api_key: "xxx"
  model: "/data/kaipeng/model/Qwen/Qwen3-235B-A22B-Instruct-2507"

# 管理员配置
admin:
  username: "admin"
  password_hash: "4baf5a3660a29ab8980b25ad2f90290a9eec46e94dbcfafbe533e4b110a76387"  # SHA256 hash of 'suanfazu2025'

# 日志配置
log:
  level: "info"  # debug, info, warn, error
  format: "json" # json, text
  output: "stdout" # stdout, file
